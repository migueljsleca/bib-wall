export type RaceEntry = {
  slug: string;
  raceName: string;
  raceType: string;
  distance: string;
  elevation: string;
  date: string;
  bibImage?: string;
  gpxFile?: string;
  photos: string[];
  notes: string;
};
